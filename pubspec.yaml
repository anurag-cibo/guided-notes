name: guided_notes
description: Ziele und Zwischenziele, ruhig und offline.
publish_to: 'none'
version: 0.3.2+8

environment:
  sdk: ^3.13.3

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  drift: ^2.35.0
  characters: ^1.4.1
  path_provider: ^2.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  sqlite3: ^3.5.2

flutter:
  uses-material-design: true
